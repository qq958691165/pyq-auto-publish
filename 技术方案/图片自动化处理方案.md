# 图片自动化处理完整方案

## 🎯 核心问题
**堆雪球发布图文朋友圈时，需要先把图片存到本地，再上传到堆雪球**

## ✅ 解决方案对比

### 方案1：影刀RPA全自动处理（⭐⭐⭐⭐⭐ 推荐）

#### 技术原理
影刀RPA可以模拟人工操作，完成图片的下载和上传全流程

#### 实现步骤

##### 1️⃣ 爬取公众号文章时提取图片URL
```javascript
// 影刀RPA - 公众号文章采集脚本
{
  "action": "爬取公众号文章",
  "steps": [
    {
      "type": "打开网页",
      "url": "公众号文章链接"
    },
    {
      "type": "提取内容",
      "selector": ".rich_media_content",
      "extract": {
        "title": "文章标题",
        "content": "文章正文",
        "images": "所有img标签的src属性"  // 关键：提取所有图片URL
      }
    }
  ]
}
```

##### 2️⃣ 自动下载图片到本地
```javascript
// 影刀RPA - 图片下载功能
{
  "action": "批量下载图片",
  "images": [
    "https://mmbiz.qpic.cn/image1.jpg",
    "https://mmbiz.qpic.cn/image2.jpg",
    "https://mmbiz.qpic.cn/image3.jpg"
  ],
  "savePath": "/Users/apple/Desktop/temp_images/",  // 临时存储路径
  "naming": "sequential"  // 按顺序命名：1.jpg, 2.jpg, 3.jpg
}
```

**影刀RPA内置下载功能**：
- 支持批量下载
- 自动处理图片格式转换
- 支持设置下载超时
- 自动重试失败的下载

##### 3️⃣ 自动上传图片到堆雪球
```javascript
// 影刀RPA - 堆雪球上传脚本
{
  "action": "发布朋友圈",
  "steps": [
    {
      "type": "打开网页",
      "url": "https://duixueqiu.com"
    },
    {
      "type": "登录",
      "method": "cookie"  // 使用保存的登录状态
    },
    {
      "type": "点击发布按钮",
      "selector": "#publish-button"
    },
    {
      "type": "填写文案",
      "selector": "#content-textarea",
      "text": "从飞书获取的文案内容"
    },
    {
      "type": "上传图片",  // 关键步骤
      "selector": "#image-upload-input",
      "files": [
        "/Users/apple/Desktop/temp_images/1.jpg",
        "/Users/apple/Desktop/temp_images/2.jpg",
        "/Users/apple/Desktop/temp_images/3.jpg"
      ]
    },
    {
      "type": "选择微信号",
      "accounts": ["微信号1", "微信号2"]
    },
    {
      "type": "设置发布时间",
      "time": "2024-10-15 18:00"
    },
    {
      "type": "点击提交",
      "selector": "#submit-button"
    }
  ]
}
```

##### 4️⃣ 清理临时文件
```javascript
// 发布完成后自动清理
{
  "action": "清理临时文件",
  "path": "/Users/apple/Desktop/temp_images/",
  "deleteAfterUpload": true
}
```

#### 优点
- ✅ **完全自动化** - 无需人工干预
- ✅ **稳定可靠** - 影刀RPA专业工具
- ✅ **易于维护** - 可视化配置
- ✅ **支持批量** - 一次处理多张图片
- ✅ **错误处理** - 自动重试机制

#### 缺点
- ⚠️ 需要购买影刀RPA专业版（约500-2000元/年）
- ⚠️ 需要本地电脑保持运行

---

### 方案2：Node.js + Puppeteer自动化（⭐⭐⭐⭐）

#### 技术原理
使用Node.js编写脚本，通过Puppeteer控制浏览器完成图片下载和上传

#### 实现代码

```javascript
// 图片自动化处理脚本
const puppeteer = require('puppeteer');
const axios = require('axios');
const fs = require('fs');
const path = require('path');

class ImageAutomation {
  constructor() {
    this.tempDir = '/Users/apple/Desktop/temp_images/';
    this.browser = null;
  }

  // 1. 下载图片
  async downloadImages(imageUrls) {
    console.log('开始下载图片...');
    
    // 确保临时目录存在
    if (!fs.existsSync(this.tempDir)) {
      fs.mkdirSync(this.tempDir, { recursive: true });
    }

    const downloadedFiles = [];

    for (let i = 0; i < imageUrls.length; i++) {
      const imageUrl = imageUrls[i];
      const fileName = `${i + 1}.jpg`;
      const filePath = path.join(this.tempDir, fileName);

      try {
        // 下载图片
        const response = await axios({
          method: 'get',
          url: imageUrl,
          responseType: 'stream'
        });

        // 保存到本地
        const writer = fs.createWriteStream(filePath);
        response.data.pipe(writer);

        await new Promise((resolve, reject) => {
          writer.on('finish', resolve);
          writer.on('error', reject);
        });

        console.log(`✅ 下载成功: ${fileName}`);
        downloadedFiles.push(filePath);

      } catch (error) {
        console.error(`❌ 下载失败: ${imageUrl}`, error.message);
      }
    }

    return downloadedFiles;
  }

  // 2. 上传图片到堆雪球
  async uploadToDuixueqiu(content, imagePaths, accounts, publishTime) {
    console.log('启动浏览器...');
    this.browser = await puppeteer.launch({
      headless: false,  // 显示浏览器窗口
      defaultViewport: null
    });

    const page = await this.browser.newPage();

    try {
      // 打开堆雪球
      console.log('打开堆雪球网站...');
      await page.goto('https://duixueqiu.com', { waitUntil: 'networkidle2' });

      // 等待登录（如果需要）
      await page.waitForTimeout(2000);

      // 点击发布按钮
      console.log('点击发布按钮...');
      await page.click('#publish-button');
      await page.waitForTimeout(1000);

      // 填写文案
      console.log('填写文案...');
      await page.type('#content-textarea', content);
      await page.waitForTimeout(500);

      // 上传图片 - 关键步骤
      console.log('上传图片...');
      const fileInput = await page.$('#image-upload-input');
      
      if (fileInput) {
        // 一次性上传所有图片
        await fileInput.uploadFile(...imagePaths);
        console.log(`✅ 已上传 ${imagePaths.length} 张图片`);
        
        // 等待图片上传完成
        await page.waitForTimeout(3000);
      } else {
        throw new Error('找不到图片上传按钮');
      }

      // 选择微信号
      console.log('选择微信号...');
      for (const account of accounts) {
        await page.click(`input[value="${account}"]`);
      }

      // 设置发布时间
      if (publishTime) {
        console.log('设置发布时间...');
        await page.type('#publish-time-input', publishTime);
      }

      // 提交发布
      console.log('提交发布...');
      await page.click('#submit-button');
      await page.waitForTimeout(2000);

      console.log('✅ 发布成功！');

    } catch (error) {
      console.error('❌ 发布失败:', error.message);
      throw error;
    } finally {
      await this.browser.close();
    }
  }

  // 3. 清理临时文件
  async cleanupTempFiles() {
    console.log('清理临时文件...');
    
    if (fs.existsSync(this.tempDir)) {
      const files = fs.readdirSync(this.tempDir);
      
      for (const file of files) {
        fs.unlinkSync(path.join(this.tempDir, file));
      }
      
      fs.rmdirSync(this.tempDir);
      console.log('✅ 临时文件已清理');
    }
  }

  // 完整流程
  async processAndPublish(imageUrls, content, accounts, publishTime) {
    try {
      // 1. 下载图片
      const imagePaths = await this.downloadImages(imageUrls);
      
      if (imagePaths.length === 0) {
        throw new Error('没有成功下载任何图片');
      }

      // 2. 上传并发布
      await this.uploadToDuixueqiu(content, imagePaths, accounts, publishTime);

      // 3. 清理临时文件
      await this.cleanupTempFiles();

      return { success: true, message: '发布成功' };

    } catch (error) {
      console.error('处理失败:', error);
      return { success: false, message: error.message };
    }
  }
}

// 使用示例
async function main() {
  const automation = new ImageAutomation();

  const imageUrls = [
    'https://mmbiz.qpic.cn/mmbiz_jpg/xxxxx/1.jpg',
    'https://mmbiz.qpic.cn/mmbiz_jpg/xxxxx/2.jpg',
    'https://mmbiz.qpic.cn/mmbiz_jpg/xxxxx/3.jpg'
  ];

  const content = '今天分享一个超实用的技巧！✨';
  const accounts = ['微信号1', '微信号2'];
  const publishTime = '2024-10-15 18:00';

  await automation.processAndPublish(imageUrls, content, accounts, publishTime);
}

module.exports = ImageAutomation;
```

#### 优点
- ✅ **完全免费** - 开源技术栈
- ✅ **高度可控** - 代码级别控制
- ✅ **灵活定制** - 可以添加任何功能
- ✅ **易于集成** - 与现有Node.js服务整合

#### 缺点
- ⚠️ 需要编写和维护代码
- ⚠️ 需要处理浏览器兼容性
- ⚠️ 需要定期更新选择器

---

### 方案3：直接使用堆雪球API（⭐⭐⭐⭐⭐ 最优方案）

#### 技术原理
如果堆雪球提供API接口，可以直接通过API上传图片，无需浏览器自动化

#### 实现代码

```javascript
// 使用堆雪球API直接上传
const axios = require('axios');
const FormData = require('form-data');
const fs = require('fs');

class DuixueqiuAPI {
  constructor(apiKey) {
    this.apiKey = apiKey;
    this.baseUrl = 'https://api.duixueqiu.com';
  }

  // 1. 下载图片到内存（不保存到本地）
  async downloadImageToBuffer(imageUrl) {
    const response = await axios({
      method: 'get',
      url: imageUrl,
      responseType: 'arraybuffer'
    });
    return Buffer.from(response.data);
  }

  // 2. 直接上传图片到堆雪球
  async uploadImage(imageBuffer, fileName) {
    const formData = new FormData();
    formData.append('image', imageBuffer, fileName);

    const response = await axios.post(
      `${this.baseUrl}/upload/image`,
      formData,
      {
        headers: {
          ...formData.getHeaders(),
          'Authorization': `Bearer ${this.apiKey}`
        }
      }
    );

    return response.data.imageId;  // 返回图片ID
  }

  // 3. 发布朋友圈
  async publishMoment(content, imageIds, accounts, publishTime) {
    const response = await axios.post(
      `${this.baseUrl}/moments/publish`,
      {
        content: content,
        images: imageIds,
        accounts: accounts,
        publishTime: publishTime
      },
      {
        headers: {
          'Authorization': `Bearer ${this.apiKey}`,
          'Content-Type': 'application/json'
        }
      }
    );

    return response.data;
  }

  // 完整流程（无需本地存储）
  async publishWithImages(imageUrls, content, accounts, publishTime) {
    try {
      console.log('开始处理图片...');
      const imageIds = [];

      // 下载并上传每张图片
      for (let i = 0; i < imageUrls.length; i++) {
        const imageUrl = imageUrls[i];
        console.log(`处理图片 ${i + 1}/${imageUrls.length}...`);

        // 下载到内存
        const imageBuffer = await this.downloadImageToBuffer(imageUrl);

        // 直接上传
        const imageId = await this.uploadImage(imageBuffer, `image_${i + 1}.jpg`);
        imageIds.push(imageId);

        console.log(`✅ 图片 ${i + 1} 上传成功`);
      }

      // 发布朋友圈
      console.log('发布朋友圈...');
      const result = await this.publishMoment(content, imageIds, accounts, publishTime);

      console.log('✅ 发布成功！');
      return { success: true, data: result };

    } catch (error) {
      console.error('❌ 发布失败:', error.message);
      return { success: false, message: error.message };
    }
  }
}

// 使用示例
async function main() {
  const api = new DuixueqiuAPI('your-api-key');

  const imageUrls = [
    'https://mmbiz.qpic.cn/mmbiz_jpg/xxxxx/1.jpg',
    'https://mmbiz.qpic.cn/mmbiz_jpg/xxxxx/2.jpg'
  ];

  await api.publishWithImages(
    imageUrls,
    '今天分享一个超实用的技巧！✨',
    ['微信号1', '微信号2'],
    '2024-10-15 18:00'
  );
}

module.exports = DuixueqiuAPI;
```

#### 优点
- ✅ **最高效** - 无需浏览器，直接API调用
- ✅ **无需本地存储** - 图片直接从内存上传
- ✅ **速度最快** - 没有浏览器渲染开销
- ✅ **最稳定** - 不受页面改版影响
- ✅ **易于监控** - API返回明确的状态码

#### 缺点
- ⚠️ 需要堆雪球提供API接口
- ⚠️ 可能需要付费API服务

---

## 🎯 推荐方案选择

### 如果堆雪球有API
**使用方案3（API直接上传）** - 最优解

### 如果堆雪球没有API
**使用方案1（影刀RPA）** - 最稳定可靠

### 如果想省钱且有技术能力
**使用方案2（Puppeteer）** - 免费且灵活

---

## 📋 完整自动化流程（推荐）

```
1. 飞书表格写入文案和公众号链接
   ↓
2. Node.js定时轮询检测到新任务
   ↓
3. 爬取公众号文章，提取图片URL
   ↓
4. 自动下载图片（方案1/2）或直接上传（方案3）
   ↓
5. 影刀RPA/Puppeteer自动登录堆雪球
   ↓
6. 自动上传图片到堆雪球
   ↓
7. 填写文案、选择账号、设置时间
   ↓
8. 自动提交发布
   ↓
9. 清理临时文件
   ↓
10. 更新飞书表格状态为"已发布"
```

**全程无需人工干预！**

---

## 🔧 技术要点

### 图片下载技术
- **axios** - HTTP请求下载图片
- **stream** - 流式处理大图片
- **buffer** - 内存中处理图片

### 图片上传技术
- **Puppeteer uploadFile()** - 浏览器自动化上传
- **FormData** - API上传图片
- **影刀RPA上传组件** - 可视化配置上传

### 防封策略
- 随机延迟（500-2000ms）
- 模拟人工操作轨迹
- 限制上传速度
- 使用代理IP（如需要）

---

## 💡 关键建议

1. **优先咨询堆雪球是否提供API** - 这是最优解
2. **如果没有API，推荐使用影刀RPA** - 稳定可靠
3. **图片临时存储路径要固定** - 便于管理和清理
4. **上传失败要有重试机制** - 提高成功率
5. **定期清理临时文件** - 避免占用磁盘空间

