# 堆雪球自动化操作指南

## 📋 目录

- [核心原则](#-核心原则)
- [Script 1: 输入链接自动发布](#-script-1-输入链接自动发布)
- [Script 4: 跟圈自动化](#-script-4-跟圈自动化)
- [关键技术要点](#-关键技术要点)
- [常见问题排查](#-常见问题排查)

---

## 🎯 核心原则

### 1. 智能检测 vs 硬编码等待

**❌ 禁止使用硬编码等待时间**:
```typescript
// 错误示例
await page.waitForTimeout(5000); // 固定等待5秒
```

**✅ 使用智能检测**:
```typescript
// 正确示例
await page.waitForFunction(() => {
  const input = document.querySelector('input[type="file"]');
  return input && input.files && input.files.length > 0;
}, { timeout: 10000 });
```

### 2. 简单逻辑优于复杂逻辑

**核心教训**: Script 1的提交按钮点击失败就是因为过度复杂的对话框查找逻辑

**❌ 复杂逻辑 (容易出错)**:
```typescript
// 查找所有对话框 → 检查可见性 → 查找特定元素 → 精确匹配文本
const dialogs = document.querySelectorAll('.el-dialog__wrapper');
for (const dialog of dialogs) {
  if (window.getComputedStyle(dialog).display === 'none') continue;
  const hasContentTextarea = dialog.querySelector('textarea[placeholder="请填写朋友圈内容"]');
  if (!hasContentTextarea) continue;
  const footer = dialog.querySelector('.el-dialog__footer');
  if (footer) {
    const buttons = footer.querySelectorAll('button');
    for (const button of buttons) {
      const text = button.textContent?.trim().replace(/\s+/g, '');
      if (text === '确定') {
        button.click();
      }
    }
  }
}
```

**✅ 简单逻辑 (稳定可靠)**:
```typescript
// 直接查找所有按钮 → 包含匹配 → 点击
const buttons = document.querySelectorAll('button');
for (const button of buttons) {
  if (button.textContent?.includes('确定')) {
    button.click();
    return;
  }
}
```

### 3. 浏览器Console日志转发

**重要**: 在`page.evaluate()`中的`console.log`不会自动显示在Node.js日志中,需要监听转发:

```typescript
// 监听浏览器console日志
page.on('console', msg => {
  const text = msg.text();
  if (text.startsWith('[Puppeteer]')) {
    this.logger.log(`🌐 ${text}`);
  }
});

// 在page.evaluate中使用
await page.evaluate(() => {
  console.log('[Puppeteer] 开始查找确定按钮...');
  const buttons = document.querySelectorAll('button');
  console.log(`[Puppeteer] 找到 ${buttons.length} 个按钮`);
});
```

---

## 📝 Script 1: 输入链接自动发布

### 功能说明

从公众号文章链接自动完成:
1. 采集文章内容和图片
2. AI智能改写生成3个版本
3. 自动发布到堆雪球朋友圈

### 操作流程

#### 步骤1: 登录堆雪球
```typescript
// 1. 访问登录页面
await page.goto('https://www.duixueqiu.com/login');

// 2. 填写账号密码
await page.type('input[placeholder="请输入账号"]', username);
await page.type('input[placeholder="请输入密码"]', password);

// 3. 点击登录按钮
await page.click('button:has-text("登录")');

// 4. 等待登录成功
await page.waitForNavigation();
```

#### 步骤2: 导航到朋友圈页面
```typescript
// 1. 点击"朋友圈"菜单
await page.click('a[href="/circle"]');

// 2. 等待页面加载
await page.waitForSelector('.circle-container');
```

#### 步骤3: 打开发朋友圈对话框
```typescript
// 1. 点击"发朋友圈"按钮
await page.click('button:has-text("发朋友圈")');

// 2. 等待对话框出现
await page.waitForSelector('.el-dialog__wrapper');
```

#### 步骤4: 选择发布类型
```typescript
// 选择"图片"类型 (不是"图文"模式)
await page.evaluate(() => {
  const labels = document.querySelectorAll('label.el-radio');
  for (const label of labels) {
    if (label.textContent?.includes('图片')) {
      (label as HTMLElement).click();
      return;
    }
  }
});
```

#### 步骤5: 上传图片
```typescript
// 1. 找到文件上传input
const fileInput = await page.$('input[type="file"]');

// 2. 上传图片文件
await fileInput.uploadFile(...imagePaths);

// 3. 智能等待上传完成
await page.waitForFunction(() => {
  const input = document.querySelector('input[type="file"]');
  return input && input.files && input.files.length > 0;
}, { timeout: 10000 });
```

#### 步骤6: 填写文案
```typescript
// 1. 找到文案输入框
const textarea = await page.$('textarea[placeholder="请填写朋友圈内容"]');

// 2. 清空并输入文案
await textarea.click({ clickCount: 3 });
await textarea.type(content);
```

#### 步骤7: 选择微小号
```typescript
// 1. 点击"选择微小号"按钮
await page.click('button:has-text("选择微小号")');

// 2. 等待微小号选择对话框出现
await page.waitForSelector('.el-dialog__title:has-text("选择微小号")');

// 3. 选择所有微小号
await page.click('label.el-checkbox:has-text("全选")');

// 4. 点击微小号对话框的"确 定"按钮 (注意有空格)
await page.evaluate(() => {
  const dialogs = document.querySelectorAll('.el-dialog__wrapper');
  for (const dialog of dialogs) {
    const title = dialog.querySelector('.el-dialog__title');
    if (title?.textContent?.includes('选择微小号')) {
      const footer = dialog.querySelector('.el-dialog__footer');
      if (footer) {
        const buttons = footer.querySelectorAll('button');
        for (const button of buttons) {
          const text = button.textContent?.trim().replace(/\s+/g, '');
          if (text === '确定') {
            (button as HTMLElement).click();
            return;
          }
        }
      }
    }
  }
});
```

#### 步骤8: 选择发送时间
```typescript
// 选择"立刻发送"
await page.evaluate(() => {
  const labels = document.querySelectorAll('label.el-radio');
  for (const label of labels) {
    if (label.textContent?.includes('立刻发送')) {
      (label as HTMLElement).click();
      return;
    }
  }
});
```

#### 步骤9: 提交发布任务 ⭐ 核心步骤
```typescript
// 使用简单逻辑点击确定按钮
await page.evaluate(() => {
  console.log('[Puppeteer] 开始查找确定按钮...');
  const buttons = document.querySelectorAll('button');
  console.log(`[Puppeteer] 找到 ${buttons.length} 个按钮`);
  
  for (let i = 0; i < buttons.length; i++) {
    const button = buttons[i];
    const text = button.textContent;
    console.log(`[Puppeteer] 按钮${i}: text="${text}"`);
    if (text && text.includes('确定')) {
      console.log(`[Puppeteer] 找到确定按钮! 准备点击...`);
      (button as HTMLElement).click();
      console.log('[Puppeteer] 确定按钮已点击!');
      return;
    }
  }
  console.log('[Puppeteer] 未找到确定按钮');
});
```

**关键要点**:
- ✅ 使用`includes('确定')`而不是精确匹配
- ✅ 不需要复杂的对话框查找逻辑
- ✅ 添加详细的console日志便于调试
- ✅ 简单直接,稳定可靠

---

## 🔄 Script 4: 跟圈自动化

### 功能说明

自动发布多条相同内容的朋友圈,并定时删除旧朋友圈:
1. 立即发布第1条朋友圈
2. 按间隔时间发布后续朋友圈
3. 发布新朋友圈前自动删除上一条
4. 最后一条朋友圈保留不删除

### 操作流程

#### 步骤1-8: 与Script 1相同

登录、导航、打开对话框、选择类型、上传图片、填写文案、选择微小号、选择发送时间

#### 步骤9: 提交发布任务

```typescript
// 提交 (与Script 1完全相同的逻辑)
await page.evaluate(() => {
  const buttons = document.querySelectorAll('button');
  for (const button of buttons) {
    if (button.textContent?.includes('确定')) {
      (button as HTMLElement).click();
      return;
    }
  }
});
```

#### 步骤10: 等待任务创建成功

```typescript
// 等待对话框关闭
await page.waitForFunction(() => {
  const dialogs = document.querySelectorAll('.el-dialog__wrapper');
  for (const dialog of dialogs) {
    if (window.getComputedStyle(dialog).display !== 'none') {
      return false;
    }
  }
  return true;
}, { timeout: 10000 });
```

#### 步骤11: 删除上一条朋友圈 (第2次及以后)

```typescript
// 1. 找到第一条朋友圈
const firstCircle = await page.$('.circle-item:first-child');

// 2. 点击删除按钮
await firstCircle.$eval('button:has-text("删除")', btn => btn.click());

// 3. 确认删除
await page.click('button:has-text("确定")');
```

---

## 🔧 关键技术要点

### 1. Element UI组件识别

堆雪球使用Element UI框架,但也有自定义组件:

**标准Element UI组件**:
- `.el-dialog__wrapper` - 对话框容器
- `.el-dialog__title` - 对话框标题
- `.el-dialog__footer` - 对话框底部
- `.el-radio` - 单选按钮
- `.el-checkbox` - 复选框

**自定义组件** (不要依赖):
- `.ac-image-upload` - 自定义图片上传组件
- `.ac-image-upload-list` - 自定义图片列表

### 2. 按钮文本匹配规则

**微小号对话框的"确 定"按钮**:
- 文本内容: `"确 定"` (中间有空格)
- 匹配方法: `text.replace(/\s+/g, '') === '确定'`

**主对话框的"确定"按钮**:
- 文本内容: `"确定"` (可能有空格,也可能没有)
- 匹配方法: `text.includes('确定')` (推荐)

### 3. 图片上传检测

**不要使用Element UI选择器**:
```typescript
// ❌ 错误 - Element UI选择器在堆雪球中不存在
await page.waitForSelector('.el-upload-list__item-thumbnail');
```

**使用文件input检测**:
```typescript
// ✅ 正确 - 检查文件input的files属性
await page.waitForFunction(() => {
  const input = document.querySelector('input[type="file"]');
  return input && input.files && input.files.length > 0;
}, { timeout: 10000 });
```

### 4. 对话框状态检测

**检查对话框是否关闭**:
```typescript
await page.waitForFunction(() => {
  const dialogs = document.querySelectorAll('.el-dialog__wrapper');
  for (const dialog of dialogs) {
    if (window.getComputedStyle(dialog).display !== 'none') {
      return false; // 还有对话框打开
    }
  }
  return true; // 所有对话框都关闭了
}, { timeout: 10000 });
```

---

## ❓ 常见问题排查

### 问题1: 提交按钮点击失败

**症状**: 日志显示"执行成功",但堆雪球中没有创建任务

**原因**: 
- 复杂的对话框查找逻辑失败
- 精确文本匹配失败

**解决方案**:
```typescript
// 使用简单的includes逻辑
await page.evaluate(() => {
  const buttons = document.querySelectorAll('button');
  for (const button of buttons) {
    if (button.textContent?.includes('确定')) {
      (button as HTMLElement).click();
      return;
    }
  }
});
```

### 问题2: 图片上传检测超时

**症状**: 日志显示"图片上传检测超时,继续执行"

**原因**: 
- 使用了不存在的Element UI选择器
- 堆雪球使用自定义上传组件

**解决方案**:
```typescript
// 检查文件input而不是Element UI组件
await page.waitForFunction(() => {
  const input = document.querySelector('input[type="file"]');
  return input && input.files && input.files.length > 0;
}, { timeout: 10000 });
```

### 问题3: 页面显示乱码 (方框)

**症状**: 截图中所有中文显示为方框

**原因**: 
- 服务器无头浏览器缺少中文字体

**影响**: 
- 不影响功能,因为我们使用`textContent`而不是视觉识别
- 只影响截图的可读性

**解决方案**:
- 无需解决,不影响自动化功能
- 如需截图可读,可安装中文字体

### 问题4: 微小号选择对话框的"确 定"按钮找不到

**症状**: 微小号选择后无法关闭对话框

**原因**: 
- 按钮文本是`"确 定"` (中间有空格)
- 使用`===`精确匹配失败

**解决方案**:
```typescript
// 方法1: 去除所有空格后匹配
const text = button.textContent?.trim().replace(/\s+/g, '');
if (text === '确定') {
  button.click();
}

// 方法2: 使用includes (推荐)
if (button.textContent?.includes('确定')) {
  button.click();
}
```

---

## 📊 调试技巧

### 1. 添加详细日志

```typescript
// Node.js日志
this.logger.log('🖱️  [步骤X] 操作描述...');

// 浏览器console日志
await page.evaluate(() => {
  console.log('[Puppeteer] 浏览器内部操作...');
});
```

### 2. 截图保存关键步骤

```typescript
// 操作前截图
await page.screenshot({ path: 'debug_before_action.png', fullPage: true });

// 操作后截图
await page.screenshot({ path: 'debug_after_action.png', fullPage: true });
```

### 3. 查看PM2日志

```bash
# 实时查看日志
ssh root@124.223.35.102 'pm2 logs pyq-backend --lines 100'

# 查看最近日志
ssh root@124.223.35.102 'pm2 logs pyq-backend --lines 200 --nostream | tail -100'

# 搜索特定内容
ssh root@124.223.35.102 'pm2 logs pyq-backend --lines 200 --nostream | grep "关键词"'
```

---

## 🎯 最佳实践总结

1. **简单逻辑优于复杂逻辑** - Script 1的修复就是最好的例子
2. **智能检测优于硬编码等待** - 使用`waitForFunction`而不是`waitForTimeout`
3. **包含匹配优于精确匹配** - 使用`includes()`而不是`===`
4. **详细日志便于调试** - 添加emoji前缀和详细描述
5. **截图保存关键步骤** - 便于排查问题
6. **浏览器日志转发** - 监听`page.on('console')`

---

<div align="center">

**Made with ❤️ by 刀仔老板团队**

最后更新: 2025-10-30

</div>

